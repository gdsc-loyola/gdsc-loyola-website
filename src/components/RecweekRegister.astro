<a
  class="recweek-cta"
  href="https://docs.google.com/forms/d/e/1FAIpQLSclUX6o7hCY0TkzNhoYmIIhyO8LSm98JD4FFGdvoPeRUs5ajA/viewform"
  target="_blank"
  rel="noopener noreferrer"
>
  Register Now</a
>

<style>
  .recweek-cta {
    cursor: pointer;
    display: block;
    width: max-content;
    position: relative;
    font-weight: 500;
    padding-left: 1.25rem;
    padding-right: 1.25rem;
    padding-top: 0.5rem;
    padding-bottom: 0.5rem;

    overflow: hidden;
  }

  .recweek-cta::before {
    display: block;
    content: '';
    position: absolute;
    inset: 0;
    z-index: -1;
    border-radius: 4px;

    padding: 2px; /* control the border thickness */
    background: linear-gradient(90deg, #498af4 0%, #eb4a3d 20.83%, #fbbc04 46.35%, #0f9d58 81.25%);

    pointer-events: none;
    -webkit-mask:
      linear-gradient(#fff 0 0) content-box,
      linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
  }
  .recweek-cta::after {
    content: '';
    position: absolute;
    inset: 0;
    transition: background-color 0.25s ease;
  }

  .recweek-cta:hover::after {
    background: #f7ebc433;
  }
</style>
